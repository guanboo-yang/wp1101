{
	"name": "final-project",
	"version": "0.3.9",
	"main": "index.js",
	"license": "MIT",
	"scripts": {
		"install:all": "for dir in */; do yarn --cwd \"${dir}\"; done",
		"start": "yarn --cwd frontend start",
		"server": "yarn --cwd backend server"
	},
	"devDependencies": {}
}
